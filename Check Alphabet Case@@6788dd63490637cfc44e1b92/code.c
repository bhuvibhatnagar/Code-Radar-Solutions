#include <stdio.h>

int main(){
    char a;
    scanf("%c",&a);
    if(c >=A && C<= Z){
        printf("Uppercase");
    }
    else if(c >= a && c<= z){
        printf("Lowercase");
    }
    else{
        printf("Not an alphabet");
    }
    return 0;
}